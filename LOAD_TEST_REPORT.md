# V2 异步导入真实压测报告

## 1. 测试信息

- 测试时间：2026-08-04
- 执行环境：Windows 本地 Node.js 22.22.2，直连最终 Vercel 环境使用的 Neon PostgreSQL
- 数据库访问：Neon HTTP + Drizzle ORM
- 处理单元：1,000 行/批，共 10 批
- Worker 并发：4
- SKU 主数据：20,000 条
- 压测文件：`test-data/10000-orders-fixed.xlsx`，10,000 行、5,000 个运单、104 条故意构造的非法 SKU
- 解析规则：数据库中已保存的标准 V2 规则，规则 ID `1b641ba8-f5de-40e7-9432-6653d8aaf006`

## 2. 最终结果

| 指标 | 实测 | 目标 | 结论 |
|---|---:|---:|---|
| 任务创建耗时 | 2,150 ms | ≤ 1,000 ms | 未达标 |
| 10,000 行全链路 | 56,798 ms | ≤ 60,000 ms | 达标 |
| 处理吞吐 | 11,536 行/分钟 | ≥ 10,000 行/分钟 | 达标 |
| 成功行 | 9,896 | — | 符合预期 |
| 失败行 | 104 | 104 条非法 SKU | 全部定位 |
| 完成批次 | 10 / 10 | 10 / 10 | 达标 |
| 行级错误 | 104 条 E001 | 可定位行和字段 | 达标 |
| 降级批次 | 0 | 正常依赖下不降级 | 达标 |
| 重复消费 | 0 个批次，687 ms | 不重复处理 | 达标 |
| 运单主表 | 5,000 条 | 5,000 个外部编码 | 达标 |
| 性能日志 | 10 条；总耗时 P50 4,192ms / P95 47,362ms | 每批 1 条 | 达标 |
| SKU 校验耗时 | P50 1,729ms / P95 2,552ms | 批量校验 | 达标 |
| 数据库写入耗时 | P50 2,248ms / P95 45,044ms | 批量写入 | 有并发长尾 |
| Outbox | 11 条 | 任务事件 1 + 批次事件 10 | 达标 |
| Trace 事件 | 23 条 | 全链路可追踪 | 达标 |

最终任务：

- `task_id`: `f71eb728-833c-43bc-84e4-87890daf5711`
- `trace_id`: `42dc18d0-be5b-453f-bad7-0eb7d6699c4a`
- 最终状态：`partial_success`

## 3. 优化过程与瓶颈

初版对 Neon HTTP 进行逐条 Outbox 更新、逐条 Trace 写入和多次分块 INSERT，数据库网络往返造成分钟级长尾。之后完成以下优化：

1. Outbox 使用 `UPDATE ... RETURNING` 原子批量认领；
2. 10 个批次事件一次性写入 Outbox；
3. 任务可复读载荷使用 gzip + base64 存储，Worker 透明解压；
4. 同一 Worker 消费进程只读取并解压一次任务上下文；
5. 每批错误、运单、SKU 明细、批次计数、性能日志、Trace 合并为单次 Neon HTTP 事务；
6. 移除多个 Worker 对 `import_tasks` 进度行的热点锁竞争，先写批次计数，最后一次聚合任务进度。

热点修复前，首波 4 个并发批次因竞争同一任务行，事务耗时约 46～49 秒；修复后端到端进入 60 秒以内。

## 4. 自动化与幂等证据

真实 Neon 集成测试共 14 项断言，已通过：

- 任务、批次、Outbox、创建 Trace 完整原子创建；
- 2 行任务产生 1 成功、1 行级错误，状态为 `partial_success`；
- 完成任务再次消费返回 0 个批次；
- 运单和错误明细计数不变；
- 超过 5 分钟的 processing 批次可恢复；
- Trace 和批次性能日志存在；
- 测试结束按精确 task_id 清理临时数据。

## 5. 未达标项与生产建议

上传接口 P95 尚未验证且单次服务层任务创建为 2.150 秒。当前浏览器先解析完整文件，再把 10,000 行 JSON 发给 API，API 又将可复读载荷写入 Neon；该架构不满足“上传原文件/对象存储引用”和“请求链路不一次性承载 10,000 行”的完整要求。

建议生产改造：

1. 浏览器直传原文件到 Vercel Blob、S3 或 R2；
2. 上传 API 只接收文件引用、规则 ID、文件哈希和预扫描行数；
3. `import_tasks` 保存对象存储 key，不保存完整结构化行 JSON；
4. Worker 从对象存储流式读取并复用 V2 规则引擎分片解析；
5. 在 Vercel + 正式队列环境执行至少 20 次上传，计算真实 P95；
6. 补充 Vercel URL、监控看板截图、数据库连接/查询日志截图。

## 6. 结论

核心异步 Worker、批量校验、批量落库、部分成功、行级错误、幂等、Outbox、卡死恢复和 Trace 已在真实 Neon 验证。10,000 行处理性能达到考试的 60 秒目标；上传接口 P95、对象存储文件引用、正式外部队列和在线 Vercel 验收仍需继续补齐，不能宣称全部考试要求已完成。
